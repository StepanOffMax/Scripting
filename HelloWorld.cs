using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // ¬вод привестви€ в игре
        print("Hello world"!);   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
